"""Core package: constants, prompt templates, AMPLE-Math loading, grading, vLLM
generation, the OPSD forward-KL loss, and the external benchmark loader.

Scripts under training/, evaluation/ and profiling/ import this package after
inserting the repository root into sys.path, so they run from any working
directory without installation (see docs/ARTIFACTS.md).
"""
